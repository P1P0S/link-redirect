<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Edit Links</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <h1>Edit Link</h1>

    <form action="/edit/<%= body.id %>" method="POST">
      <input class="field" type="text" name="title" placeholder="Title" value="<%= body.title %>" required>
      <input class="field" type="text" name="description" placeholder="Description" value="<%= body.description %>">
      <input class="field" type="url" name="url" placeholder="URL" value="<%= body.url %>" required>
      <button type="submit">Save</button>
    </form>

    <% if (error) {%>
      <h3><%= error.message %></h3>
    <% } %>
  </body>
</html>
